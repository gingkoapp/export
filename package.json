{
  "name": "gingko-export",
  "version": "0.3.7",
  "repository": "git://github.com:gingkoapp/export.git",
  "description": "Export gingko tree",
  "scripts": {
    "test": "mocha && component-testem",
    "stat": "cloc lib/ --quiet --by-file && cloc test/*.js --quiet --by-file"
  },
  "dependencies": {
    "marked": "git+ssh://git@github.com/gingkoapp/marked#0.2.10-19",
    "underscore": "~1.6.0"
  },
  "devDependencies": {
    "mocha": "*",
    "chai": "*",
    "component": "1.0.0-rc5"
  }
}
